<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <%- include('./partials/header.ejs') %>

    
    <div class="m-16 flex">
      <div class="w-2/3">
          <div class="flex justify-between items-center">
        <h2 class="text-2xl font-medium text-gray-700"><span class="text-red-800">User Profile</span> Details</h2>
        <button
          class="block rounded-lg bg-indigo-600 px-5 py-3 text-sm font-medium text-white transition hover:bg-indigo-700 focus:outline-none focus:ring mr-16"
          type="button"
        >
        <a href="/user/profile/update" class="inline-block ">Update details</a>
          
        </button>
      </div>
        <div class="flow-root mt-8">
            <dl class="-my-3 divide-y divide-gray-100 text-sm">
              
          
              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">Name</dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].fullName %></dd>
              </div>
          
              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">Email</dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].email %></dd>
              </div>

              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">Profile Image</dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].profileImageURL %></dd>
              </div>
          
              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">Role</dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].role %></dd>
              </div>
          
              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">Bio</dt>
                <dd class="text-blue-700 sm:col-span-2 font-semibold">
                    <% if (profileDetails[0]?.bio) { %>
                      <%= profileDetails[0].bio %>
                      <% } else { %>
                        <h4 class="hover:underline font-semibold">Edit your bio</h4>
                      <% } %>
                      
                </dd>
              </div>

              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">account created </dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].createdAt.toString().slice(3, profileDetails[0].createdAt.toString().indexOf("2024") + 4) %></dd>
              </div>

              <div class="grid grid-cols-1 gap-1 py-3 sm:grid-cols-3 sm:gap-4">
                <dt class="font-medium text-red-800">account updated </dt>
                <dd class="text-gray-700 sm:col-span-2 font-semibold"><%= profileDetails[0].updatedAt.toString().slice(3, profileDetails[0].updatedAt.toString().indexOf("2024") + 4) %></dd>
              </div>
            </dl>
          </div>

        </div>    

        <div class=" border-2 border-sky-500">
          <img src="https://th.bing.com/th/id/OIP.8Rbxh5xsE6cPuk5jCeo6jAHaHa?rs=1&pid=ImgDetMain" alt="">
        </div>
          </div>



    <%- include('./partials/homefooter.ejs') %>
</body>
</html>